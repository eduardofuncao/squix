networks:
  default:
    name: sqlserver_default
    external: true

services:
  ttyd:
    build: .
    ports:
      - "8080:8080"
    volumes:
      # Mount workspace for presentations and files
      - ./workspace:/home/scrat/tree
    environment:
      - EDITOR=vim
    restart: unless-stopped
